<% if user_signed_in? %>
    <%= render "profile"%>
<% else %>
    <p class="w-[30%] text-center h-[10vh] p-2 bg-white opacity-60">Welcome!, Please Log In to see the usernames and make a post</p>
<% end %>

<%= render "display"%>